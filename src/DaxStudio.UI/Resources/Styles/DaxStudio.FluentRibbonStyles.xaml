<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Fluent="clr-namespace:Fluent;assembly=Fluent"
                    xmlns:converters="clr-namespace:Fluent.Converters;assembly=Fluent"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:ctrl="clr-namespace:DaxStudio.UI.Controls"
                    mc:Ignorable="d"
                    >
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary  Source="pack://application:,,,/DaxStudio.UI;Component/Resources/Styles/FluentRibbon/Menu.xaml"/>
        <!--<ResourceDictionary  Source="pack://application:,,,/DaxStudio.UI;Component/Resources/Styles/FluentRibbon/MenuItem.xaml"/>
        <ResourceDictionary  Source="pack://application:,,,/DaxStudio.UI;Component/Resources/Styles/FluentRibbon/RibbonTabControl.xaml"/>-->
    </ResourceDictionary.MergedDictionaries>

    <DrawingImage x:Key="Fluent.Ribbon.Images.RibbonDisplayOptions"  Drawing="{DynamicResource expandDrawingGroup}" />
    <DrawingImage x:Key="Fluent.Ribbon.Images.RibbonCollapse"  Drawing="{DynamicResource collapseDrawingGroup}" />
    <DrawingImage x:Key="Fluent.Ribbon.Images.RibbonExpand"  Drawing="{DynamicResource expandDrawingGroup}" />

    <!-- v11 added a rounded, floating content area (corner radius + side margins + drop shadow).
         Override those values to restore the flat, flush ribbon appearance. -->
    <CornerRadius x:Key="Fluent.Ribbon.Values.RibbonTabControl.Content.CornerRadius">0</CornerRadius>
    <Thickness x:Key="Fluent.Ribbon.Values.RibbonTabControl.Content.Margin">0</Thickness>
    <DropShadowEffect x:Key="Fluent.Ribbon.Values.RibbonTabControl.Content.DropShadowEffect"
                      BlurRadius="0" ShadowDepth="0" Opacity="0" />

    <!-- v11 removed the rounded corners + drop shadow from ribbon drop-down / split-button popups
         (its ResizeableContentControl template uses a plain square PART_DropDownBorder). Re-template
         it here to restore the previous rounded, shadowed look for ALL ribbon drop-down popups
         (theme-independent, so it applies to Light / Dark / HighContrast). -->
    <ControlTemplate x:Key="Fluent.Ribbon.Templates.ResizeableContentControl" TargetType="{x:Type Fluent:ResizeableContentControl}">
        <Border Background="Transparent" Padding="8 2 8 8">
            <Border x:Name="PART_DropDownBorder"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8"
                    SnapsToDevicePixels="True">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="{DynamicResource Theme.DropShadow.Opacity}" />
                </Border.Effect>
                <Grid Margin="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ContentPresenter />
                    <Border x:Name="resizeBorder" Grid.Row="1" Height="11"
                            HorizontalAlignment="Stretch" VerticalAlignment="Top"
                            Background="{DynamicResource Fluent.Ribbon.Brushes.DropDown.Resize.Background}"
                            BorderBrush="{DynamicResource Fluent.Ribbon.Brushes.DropDown.Resize.Border}"
                            BorderThickness="0 1 0 0">
                        <Grid>
                            <Thumb x:Name="PART_ResizeVerticalThumb" Height="10" Margin="0 0 0 -90"
                                   HorizontalAlignment="Stretch" VerticalAlignment="Top" Cursor="SizeNS"
                                   Template="{DynamicResource Fluent.Ribbon.Templates.ResizeVerticalThumb}" />
                            <Thumb x:Name="PART_ResizeBothThumb" Width="10" Height="10" Margin="0 0 0 -90"
                                   HorizontalAlignment="Right" VerticalAlignment="Top" Cursor="SizeNWSE"
                                   Template="{DynamicResource Fluent.Ribbon.Templates.ResizeBothThumb}" />
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="ResizeMode" Value="None">
                <Setter TargetName="resizeBorder" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="ResizeMode" Value="Both">
                <Setter TargetName="PART_ResizeBothThumb" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_ResizeVerticalThumb" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="resizeBorder" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="ResizeMode" Value="Vertical">
                <Setter TargetName="PART_ResizeBothThumb" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="PART_ResizeVerticalThumb" Property="Visibility" Value="Visible" />
                <Setter TargetName="resizeBorder" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- v11 redesigned the backstage tab items to use only a small left indicator bar (no selected
         background) and only grew the bar to full height when a tab was BOTH selected and hovered.
         This override restores a v9-style selected look (light background highlight) AND keeps a
         full-height left indicator, shows the larger indicator on hover of ANY tab, and animates
         the indicator (grow + fade) so it appears to move between tabs. -->
    <ControlTemplate x:Key="Fluent.Ribbon.Templates.BackstageTabItem" TargetType="{x:Type Fluent:BackstageTabItem}">
        <Border x:Name="rootBorder" BorderThickness="2" ToolTip="{TemplateBinding ToolTip}">
            <Grid x:Name="rootGrid" Background="Transparent">

                <!-- selection background highlight (restores the v9 look) -->
                <Border x:Name="bgBorder" Background="Transparent" />

                <!-- subtle background highlight shown on hover: same shade as the selected
                     item (IsSelectedBackground) but faded to a lower opacity -->
                <Border x:Name="hoverBg"
                        Background="{Binding Path=(Fluent:RibbonProperties.IsSelectedBackground), RelativeSource={RelativeSource TemplatedParent}}"
                        Opacity="0" />

                <!-- left indicator shown when the tab is selected -->
                <Border x:Name="selIndicator"
                        Width="4"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Stretch"
                        Background="{Binding Path=(Fluent:RibbonProperties.IsSelectedBackground), RelativeSource={RelativeSource TemplatedParent}}"
                        RenderTransformOrigin="0.5,0.5"
                        Opacity="0">
                    <Border.RenderTransform>
                        <ScaleTransform ScaleY="0" />
                    </Border.RenderTransform>
                </Border>

                <!-- left indicator shown when a non-selected tab is hovered -->
                <Border x:Name="hoverIndicator"
                        Width="4"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Stretch"
                        Background="{Binding Path=(Fluent:RibbonProperties.IsSelectedBackground), RelativeSource={RelativeSource TemplatedParent}}"
                        RenderTransformOrigin="0.5,0.5"
                        Opacity="0">
                    <Border.RenderTransform>
                        <ScaleTransform ScaleY="0" />
                    </Border.RenderTransform>
                </Border>

                <StackPanel Margin="25 0 15 0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Orientation="Horizontal">
                    <Fluent:IconPresenter x:Name="iconImage"
                                          Margin="0 0 8 0"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          SmallIcon="{Binding Icon, RelativeSource={RelativeSource TemplatedParent}}"
                                          CustomSize="{Binding Path=(Fluent:RibbonProperties.CustomIconSize), RelativeSource={RelativeSource TemplatedParent}}"
                                          IconSize="{Binding Path=(Fluent:RibbonProperties.IconSize), RelativeSource={RelativeSource TemplatedParent}}" />

                    <ContentControl x:Name="PART_Header"
                                    Foreground="{DynamicResource Fluent.Ribbon.Brushes.BackstageTabItem.Header.Foreground}"
                                    Content="{TemplateBinding Header}"
                                    ContentTemplate="{TemplateBinding HeaderTemplate}"
                                    ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                    Focusable="False">
                        <ContentControl.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                <Setter Property="TextWrapping" Value="NoWrap" />
                            </Style>
                        </ContentControl.Resources>
                    </ContentControl>
                </StackPanel>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <!-- these entries have no icon; collapse the icon presenter so the text left-aligns
                 identically to the action buttons (which otherwise reserve icon width) -->
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter TargetName="iconImage" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="PART_Header" Property="Opacity" Value="0.5" />
            </Trigger>

            <!-- hover on a NON-selected tab: grow the (larger) hover indicator -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition SourceName="rootGrid" Property="IsMouseOver" Value="True" />
                    <Condition Property="Selector.IsSelected" Value="False" />
                </MultiTrigger.Conditions>
                <MultiTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="hoverBg" Storyboard.TargetProperty="Opacity"
                                             To="0.5" Duration="0:0:0.12" />
                            <DoubleAnimation Storyboard.TargetName="hoverIndicator" Storyboard.TargetProperty="Opacity"
                                             To="0.8" Duration="0:0:0.12" />
                            <DoubleAnimation Storyboard.TargetName="hoverIndicator"
                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="1" Duration="0:0:0.15">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.EnterActions>
                <MultiTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="hoverBg" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="0:0:0.1" />
                            <DoubleAnimation Storyboard.TargetName="hoverIndicator" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="0:0:0.1" />
                            <DoubleAnimation Storyboard.TargetName="hoverIndicator"
                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="0" Duration="0:0:0.1" />
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.ExitActions>
            </MultiTrigger>

            <!-- selected tab: light background highlight + grow the (larger) selected indicator -->
            <Trigger Property="Selector.IsSelected" Value="True">
                <Setter TargetName="bgBorder" Property="Background"
                        Value="{Binding Path=(Fluent:RibbonProperties.IsSelectedBackground), RelativeSource={RelativeSource TemplatedParent}}" />
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="selIndicator" Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="0:0:0.15" />
                            <DoubleAnimation Storyboard.TargetName="selIndicator"
                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="1" Duration="0:0:0.18">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="selIndicator" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="0:0:0.1" />
                            <DoubleAnimation Storyboard.TargetName="selIndicator"
                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="0" Duration="0:0:0.1" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>

            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="rootBorder" Property="BorderBrush" Value="{DynamicResource Fluent.Ribbon.Brushes.Black}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Backstage "Button" entries (New, Save, Save As, Close, ...) use a different template than the
         BackstageTabItem entries (Open, Options). Override it to match: a full-height, light-blue,
         animated left indicator on hover so every backstage menu entry looks/behaves the same. Buttons
         are actions (never "selected"), so only the hover indicator applies. -->
    <ControlTemplate x:Key="Fluent.Ribbon.Templates.BackstageTabControl.Button" TargetType="{x:Type Fluent:Button}">
        <Border x:Name="rootBorder" BorderThickness="2" ToolTip="{TemplateBinding ToolTip}">
            <Grid x:Name="rootGrid" Background="Transparent">

                <!-- subtle background highlight shown on hover: same shade as the selected
                     item (IsSelectedBackground) but faded to a lower opacity -->
                <Border x:Name="hoverBg"
                        Background="{Binding Path=(Fluent:RibbonProperties.IsSelectedBackground), RelativeSource={RelativeSource TemplatedParent}}"
                        Opacity="0" />

                <Border x:Name="hoverIndicator"
                        Width="4"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Stretch"
                        Background="{Binding Path=(Fluent:RibbonProperties.IsSelectedBackground), RelativeSource={RelativeSource TemplatedParent}}"
                        RenderTransformOrigin="0.5,0.5"
                        Opacity="0">
                    <Border.RenderTransform>
                        <ScaleTransform ScaleY="0" />
                    </Border.RenderTransform>
                </Border>

                <StackPanel Margin="25 0 15 0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Orientation="Horizontal">
                    <Fluent:IconPresenter x:Name="iconImage"
                                          Margin="0 0 8 0"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          SmallIcon="{Binding Icon, RelativeSource={RelativeSource TemplatedParent}}"
                                          CustomSize="{Binding Path=(Fluent:RibbonProperties.CustomIconSize), RelativeSource={RelativeSource TemplatedParent}}"
                                          IconSize="{Binding Path=(Fluent:RibbonProperties.IconSize), RelativeSource={RelativeSource TemplatedParent}}" />

                    <ContentControl x:Name="controlLabel"
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding Header}"
                                    Focusable="False">
                        <ContentControl.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                <Setter Property="TextWrapping" Value="NoWrap" />
                            </Style>
                        </ContentControl.Resources>
                    </ContentControl>
                </StackPanel>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <!-- these entries have no icon; collapse the icon presenter so the text left-aligns
                 identically to the BackstageTabItem entries -->
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter TargetName="iconImage" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger SourceName="rootGrid" Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="hoverBg" Storyboard.TargetProperty="Opacity"
                                             To="0.5" Duration="0:0:0.12" />
                            <DoubleAnimation Storyboard.TargetName="hoverIndicator" Storyboard.TargetProperty="Opacity"
                                             To="0.8" Duration="0:0:0.12" />
                            <DoubleAnimation Storyboard.TargetName="hoverIndicator"
                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="1" Duration="0:0:0.15">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="hoverBg" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="0:0:0.1" />
                            <DoubleAnimation Storyboard.TargetName="hoverIndicator" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="0:0:0.1" />
                            <DoubleAnimation Storyboard.TargetName="hoverIndicator"
                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="0" Duration="0:0:0.1" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="rootBorder" Property="BorderBrush" Value="{DynamicResource Fluent.Ribbon.Brushes.Black}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="controlLabel" Property="Opacity" Value="0.5" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Explicit style so the backstage action buttons (New, Save, Save As, Close, ...) use the same
         animated left-indicator template as the BackstageTabItem entries. The stock Backstage.Button
         style sets no template, so these buttons otherwise fall back to the default button look. -->
    <Style x:Key="DaxStudioBackstageMenuButtonStyle" TargetType="{x:Type Fluent:Button}">
        <Setter Property="Fluent:RibbonProperties.IsSelectedBackground" Value="{DynamicResource Fluent.Ribbon.Brushes.BackstageTabItem.Selected.Background}" />
        <Setter Property="Foreground" Value="{DynamicResource Fluent.Ribbon.Brushes.Backstage.BackButton.Foreground}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Height" Value="30" />
        <Setter Property="Template" Value="{StaticResource Fluent.Ribbon.Templates.BackstageTabControl.Button}" />
    </Style>

    <!-- v11's back button hover paints a solid MouseOverBackground box, which renders as a heavy dark
         tile. Instead, use the same subtle hover overlay as the other backstage menu entries: the
         Selected.Background brush faded in to 0.5 opacity, without dimming the arrow/ellipse. -->
    <ControlTemplate x:Key="Fluent.Ribbon.Templates.BackstageBackButton" TargetType="Button">
        <Grid x:Name="ContentGrid"
              Background="Transparent"
              Fluent:FrameworkHelper.UseLayoutRounding="False">
            <Border x:Name="hoverBg"
                    Background="{DynamicResource Fluent.Ribbon.Brushes.BackstageTabItem.Selected.Background}"
                    Opacity="0" />
            <ContentControl Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="Left"
                            Focusable="False">
                <Grid>
                    <Path x:Name="arrowpath"
                          Width="16"
                          Height="12"
                          Data="F1 M 36,22L 36,26L 19.75,26L 27,33L 20.5,33L 11,24L 20.5,15L 27,15L 19.75,22L 36,22 Z "
                          Fill="{TemplateBinding Foreground}"
                          Stretch="Uniform" />
                    <Ellipse x:Name="backellipse"
                             Width="34"
                             Height="34"
                             Stroke="{TemplateBinding Foreground}"
                             StrokeThickness="2" />
                    <Path Margin="0"
                          HorizontalAlignment="Stretch"
                          Data="M0,0 C0,35 0,35 0,35 C0,35 35,35 35,35 L35,0 z"
                          Fill="Transparent"
                          Stretch="None" />
                </Grid>
            </ContentControl>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="hoverBg" Storyboard.TargetProperty="Opacity"
                                             To="0.5" Duration="0:0:0.12" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="hoverBg" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="0:0:0.1" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="hoverBg" Property="Opacity" Value="0.5" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate TargetType="{x:Type Fluent:RibbonTabItem}"
                       x:Key="DaxStudioRibbonTabItemControlTemplate">
    <Grid x:Name="grid"
          Height="Auto">
      <Border x:Name="groupBorder"
              Margin="0,0,0,1"
              BorderThickness="0"
              Background="{Binding Group.Background, RelativeSource={RelativeSource TemplatedParent}, FallbackValue=Transparent}"
              OpacityMask="{DynamicResource Fluent.Ribbon.Brushes.RibbonTabItem.Contextual.Background.OpacityMask}" />
      <Border x:Name="selectedBorder"
              Visibility="Collapsed"
              VerticalAlignment="Stretch"
              BorderBrush="{DynamicResource Fluent.Ribbon.Brushes.AccentBaseColorBrush}"
              BorderThickness="0,0,0,3"
              Margin="8,0,8,1"
              Background="{TemplateBinding ActiveTabBackground}" />

      <Border x:Name="mouseOverBorder"           
              BorderThickness="0,0,0,3"
              Margin="2,0,2,1"
              Background="{TemplateBinding Background}"
               />
      <Border x:Name="PART_ContentContainer"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              Padding="{TemplateBinding HeaderPadding}">
        <ContentControl x:Name="PART_HeaderContentHost"
                        Focusable="False"
                        AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource TemplatedParent}}"
                        AutomationProperties.AutomationId="{TemplateBinding Name}"
                        Content="{TemplateBinding Header}"
                        ContentTemplate="{TemplateBinding HeaderTemplate}" />
      </Border>
      <Grid x:Name="separatorGrid"
            Margin="0"
            Opacity="{TemplateBinding SeparatorOpacity}"
            HorizontalAlignment="Right"
            Width="1">
        <Rectangle x:Name="separatorRectangle1"
                   Fill="{TemplateBinding BorderBrush}"
                   Stroke="{TemplateBinding BorderBrush}"
                   Margin="0"
                   Width="1">
        </Rectangle>
      </Grid>
    </Grid>

    <ControlTemplate.Triggers>
      <Trigger Property="IsEnabled"
               Value="False">
        <Setter Property="Opacity"
                TargetName="PART_ContentContainer"
                Value="0.5" />
      </Trigger>
      <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Fluent:RibbonTabControl}, Path=SelectedItem}"
                   Value="{x:Null}">
        <Setter TargetName="groupBorder"
                Property="Margin"
                Value="0" />
      </DataTrigger>
      <Trigger Property="Group"
               Value="{x:Null}">
        <Setter Property="Visibility"
                TargetName="groupBorder"
                Value="Collapsed" />
      </Trigger>
      <Trigger Property="Selector.IsSelected"
               Value="True">
        <Setter Property="Visibility"
                TargetName="selectedBorder"
                Value="Visible" />
        <Setter
              TargetName="PART_HeaderContentHost"
              Property="FontWeight"
              Value="Bold"/>
       </Trigger>
      <!-- Foreground for Selected, with group -->
      <Trigger Property="Selector.IsSelected"
               Value="True">
        <Setter Property="Foreground"
                Value="{Binding Group.TabItemSelectedForeground, RelativeSource={RelativeSource Self}}" />
      </Trigger>
      <!-- Foreground for selected, no group -->
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="Selector.IsSelected"
                     Value="True" />
          <Condition Property="Group"
                     Value="{x:Null}" />
        </MultiTrigger.Conditions>
        <Setter Property="Foreground"
                Value="{DynamicResource Fluent.Ribbon.Brushes.RibbonTabItem.Selected.Foreground}" />
      </MultiTrigger>
      <!-- Background and foreground for MouseOver, with group -->
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="IsMouseOver"
                     SourceName="grid"
                     Value="True" />
        </MultiTrigger.Conditions>
        <Setter Property="Background"
                Value="{Binding Path=(Fluent:RibbonProperties.MouseOverBackground), RelativeSource={RelativeSource Self}}" />
        <Setter Property="Foreground"
                Value="{Binding Group.TabItemMouseOverForeground, RelativeSource={RelativeSource Self}}" />
                <Setter Property="BorderBrush" 
                        TargetName="mouseOverBorder"
                        Value="{DynamicResource Fluent.Ribbon.Brushes.AccentBaseColorBrush}"/>
        </MultiTrigger>
        <!-- Foreground for selected MouseOver, with group -->
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="IsMouseOver"
                     SourceName="grid"
                     Value="True" />
          <Condition Property="Selector.IsSelected"
                     Value="True" />
        </MultiTrigger.Conditions>
        <Setter Property="Foreground"
                Value="{Binding Group.TabItemSelectedMouseOverForeground, RelativeSource={RelativeSource Self}}" />
      </MultiTrigger>
      <!-- Background and foreground for MouseOver, no group -->
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="IsMouseOver"
                     SourceName="grid"
                     Value="True" />
          <Condition Property="Group"
                     Value="{x:Null}" />
        </MultiTrigger.Conditions>
                <Setter Property="Background"
                Value="{Binding Path=(Fluent:RibbonProperties.MouseOverBackground), RelativeSource={RelativeSource Self}}" />
        <Setter Property="Foreground"
                Value="{Binding Path=(Fluent:RibbonProperties.MouseOverForeground), RelativeSource={RelativeSource Self}}" />
      </MultiTrigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>


    
    <Style
       TargetType="{x:Type Fluent:RibbonTabItem}"
         BasedOn="{StaticResource Fluent.Ribbon.Styles.RibbonTabItem}">
        <Setter Property="Template"
        Value="{DynamicResource DaxStudioRibbonTabItemControlTemplate}" />
    </Style>

    <Style TargetType="{x:Type Fluent:RibbonTabControl}"
           BasedOn="{StaticResource {x:Type Fluent:RibbonTabControl}}">
        <Setter Property="Margin" Value="0 0 0 3"/>
    </Style>
    
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <ControlTemplate x:Key="RibbonWindowControlTemplate"
                     TargetType="{x:Type Fluent:RibbonWindow}">
        <!--CornerRadius="8" 
                ClipToBounds="True"-->
        <!-- Background="{DynamicResource Fluent.Ribbon.Brushes.RibbonTabControl.Content.Background}" -->
            <ctrl:ClipBorder
                CornerRadius="0"
                Background="Transparent">
            <Grid LayoutTransform="{Binding LayoutTransform, RelativeSource={RelativeSource TemplatedParent}}"
              RenderTransform="{Binding RenderTransform, RelativeSource={RelativeSource TemplatedParent}}">
                <AdornerDecorator x:Name="Adorner">
                    <!-- CornerRadius="8" 
                    ClipToBounds="True"-->
                <Border 
                    Background="{DynamicResource Fluent.Ribbon.Brushes.RibbonTabControl.Content.Background}">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <DockPanel Grid.Row="0"
                               Height="{TemplateBinding TitleBarHeight}"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Top"
                               Background="{TemplateBinding TitleBackground}"
                               LastChildFill="True">
                                <!-- Source="{Fluent:IconConverter {Binding Icon, RelativeSource={RelativeSource TemplatedParent}},
                                                             {Binding RelativeSource={RelativeSource Self}}}"-->
                            <Image x:Name="PART_Icon"
                               Width="36"
                               Height="16"
                               Margin="6 2 2 0"
                               VerticalAlignment="{TemplateBinding VerticalIconAlignment}"
                               DockPanel.Dock="Left"
                               SnapsToDevicePixels="True"
                                   Source="{DynamicResource logo_smallDrawingImage}"
                               
                               Stretch="Uniform"
                               Visibility="{TemplateBinding IsIconVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />

                            <ContentPresenter x:Name="PART_WindowCommands"
                                          Grid.Row="0"
                                          HorizontalAlignment="Right"
                                          Panel.ZIndex="1"
                                          Content="{TemplateBinding WindowCommands}"
                                          DockPanel.Dock="Right" />

                            <Fluent:RibbonTitleBar x:Name="PART_RibbonTitleBar"
                                               Foreground="{TemplateBinding TitleForeground}"
                                               Header="{TemplateBinding Title}"
                                               IsCollapsed="{TemplateBinding IsCollapsed}" />
                        </DockPanel>

                        <ContentPresenter x:Name="PART_ContentPresenter"
                                      Grid.Row="1" />
                    </Grid>
                </Border>
            </AdornerDecorator>
                <!-- 
                Cannot set CornerRadius to 8 since we need to set the application window to AllowsTransparency=true and this breaks 
                the AvalonDock unpinned tool windows 
                -->
                <ctrl:ClipBorder x:Name="PART_Border"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="{x:Null}"
                    ClipToBounds="True"
                    CornerRadius="0"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    UseLayoutRounding="True" />

                <ResizeGrip x:Name="WindowResizeGrip"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        IsTabStop="false"
                        Visibility="Collapsed" />
            </Grid>
        </ctrl:ClipBorder>

        <ControlTemplate.Triggers>
                <Trigger Property="IsActive" Value="False">
                    <Setter TargetName="PART_Border" Property="BorderBrush" Value="{Binding Path=NonActiveBorderBrush, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                    <Setter TargetName="PART_WindowCommands" Property="Opacity" Value="0.5" />
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="ResizeMode" Value="CanResizeWithGrip" />
                        <Condition Property="WindowState" Value="Normal" />
                    </MultiTrigger.Conditions>
                    <Setter TargetName="PART_Border" Property="Visibility" Value="Visible" />
                    <Setter TargetName="WindowResizeGrip" Property="Visibility" Value="Visible" />
                </MultiTrigger>
                <Trigger Property="WindowState" Value="Maximized">
                    <Setter TargetName="Adorner" Property="Margin" Value="0" />
                    <Setter TargetName="PART_Border" Property="Visibility" Value="Collapsed" />
                    <Setter TargetName="PART_Icon" Property="Margin" Value="2 2 0 0" />
                    <Setter TargetName="WindowResizeGrip" Property="Visibility" Value="Collapsed" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

    <Style x:Key="DaxStudioRibbonWindowStyle"
           TargetType="{x:Type Fluent:RibbonWindow}">
            <!--<Setter Property="Background" Value="{DynamicResource WhiteBrush}" />-->
            <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Fluent.Ribbon.Brushes.AccentBaseColorBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <!--<Setter Property="Foreground" Value="{DynamicResource BlackBrush}" />-->
            
            <Setter Property="NonActiveBorderBrush" Value="#434346" />
            <Setter Property="Template" Value="{StaticResource RibbonWindowControlTemplate}" />
            <Setter Property="TitleBackground" Value="{DynamicResource Fluent.Ribbon.Brushes.RibbonWindow.TitleBackground}" />
            <Setter Property="TitleBarHeight" Value="{DynamicResource {x:Static SystemParameters.WindowCaptionHeightKey}}" />
            <Setter Property="TitleForeground" Value="{DynamicResource Fluent.Ribbon.Brushes.RibbonWindow.TitleForeground}" />
        <!--<Setter Property="Source" TargetName="PART_Icon" Value="{DynamicResource logo_smallDrawingImage}"/>-->
    </Style>


</ResourceDictionary>