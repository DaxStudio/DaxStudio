<controls:ZoomableUserControl   x:Class="DaxStudio.UI.Views.QueryPlanTraceView"
             xmlns:controls="clr-namespace:DaxStudio.UI.Controls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:DaxStudio.UI.Converters"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro.Platform"
             xmlns:behaviours="clr-namespace:DaxStudio.UI.Behaviours"
             xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives;assembly=ModernWpf"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:ap="clr-namespace:DaxStudio.UI.AttachedProperties"
             xmlns:ctrls="clr-namespace:DaxStudio.Controls;assembly=DaxStudio.Controls"
             xmlns:filter="clr-namespace:DaxStudio.Controls.DataGridFilter;assembly=DaxStudio.Controls.DataGridFilter"
             xmlns:uictrl="clr-namespace:DaxStudio.UI.Controls"
             xmlns:local="clr-namespace:DaxStudio.UI.Resources" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600" >
    <UserControl.Resources>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>

                <!-- Design-time only fallback - loads Light theme in designer -->
                <local:DesignTimeResourceDictionary 
                    DesignTimeSource="pack://application:,,,/DaxStudio.UI;component/Resources/DesignTimeResources.xaml"/>

                <ResourceDictionary  Source="pack://application:,,,/DaxStudio.UI;Component/Resources/DaxStudioResources.xaml" />
                <ResourceDictionary  Source="pack://application:,,,/DaxStudio.UI;Component/Resources/Styles/LabelSpinnerStyle.xaml" />
                <ResourceDictionary Source="pack://application:,,,/DaxStudio.UI;Component/Resources/Styles/DaxStudio.Spinner.xaml"/>
                <ResourceDictionary  Source="pack://application:,,,/DaxStudio.UI;Component/Resources/Styles/BusyPanel.xaml" />
                <ResourceDictionary  Source="pack://application:,,,/DaxStudio.UI;Component/Resources/Styles/TraceWatcherStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <conv:BoolToHiddenConverter x:Key="BoolToVisibilityConverter" />
            <conv:BoolToCollapsedConverter x:Key="BoolToCollapsedConverter" />
            <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
            <conv:QueryPlanSelectionConverter x:Key="QueryPlanSelectionConverter" />
            <conv:QueryPlanMarginConverter x:Key="QueryPlanMarginConverter"/>
            <conv:BoolToGridRowHeightConverter x:Key="BoolToGridRowHeightConverter"/>
            <conv:AncestorRecordsColorConverter x:Key="AncestorRecordsColorConverter" />
            <conv:AncestorRecordsImageResourceConverter x:Key="AncestorRecordsImageResourceConverter" />

            
            <controls:BindingProxy x:Key="Proxy" Data="{Binding PhysicalQueryPlanTree}" />

            <Style x:Key="AlignRight" TargetType="{x:Type TextBlock}">
                <Setter Property="Padding" Value="4 4 8 4"/>
                <Setter Property="HorizontalAlignment" Value="Right" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style x:Key="AlignLeft" TargetType="{x:Type TextBlock}">
                <Setter Property="Padding" Value="4 4 8 4"/>
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>

            <sys:String x:Key="IndentSize">14</sys:String>
            <sys:String x:Key="BorderIndentSize">14 5</sys:String>

            <Style x:Key="DefaultDataGridRowStyle" TargetType="DataGridRow">
                <Setter Property="OverridesDefaultStyle" Value="True" />
                <Setter Property="SnapsToDevicePixels" Value="true" />
                <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
                <EventSetter Event="Control.RequestBringIntoView" Handler="DataGrid_RequestBringIntoView"  />
                <Setter Property="ValidationErrorTemplate">
                    <Setter.Value>
                        <ControlTemplate>
                            <TextBlock
                        Margin="2,0,0,0"
                        VerticalAlignment="Center"
                        Foreground="Red"
                        Text="!" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridRow">
                            <Grid>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <primitives:VisualStateGroupListener.Listener>
                                            <primitives:VisualStateGroupListener x:Name="CommonStatesListener" />
                                        </primitives:VisualStateGroupListener.Listener>
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="Normal_AlternatingRow" />
                                        <VisualState x:Name="MouseOver" />
                                        <VisualState x:Name="Normal_Selected" />
                                        <VisualState x:Name="MouseOver_Selected" />
                                        <VisualState x:Name="MouseOver_Unfocused_Selected" />
                                        <VisualState x:Name="Unfocused_Selected" />
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Rectangle
                            x:Name="BackgroundRectangle"
                            Fill="{TemplateBinding Background}" />
                                <Border
                            x:Name="DGR_Border"
                            Background="Transparent"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                                    <SelectiveScrollingGrid>
                                        <SelectiveScrollingGrid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </SelectiveScrollingGrid.ColumnDefinitions>
                                        <SelectiveScrollingGrid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </SelectiveScrollingGrid.RowDefinitions>
                                        <DataGridCellsPresenter
                                    x:Name="CellsPresenter"
                                    Grid.Column="1"
                                    ItemsPanel="{TemplateBinding ItemsPanel}"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                        <Border
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    Background="{DynamicResource DataGridDetailsPresenterBackgroundBrush}">
                                            <DataGridDetailsPresenter
                                        x:Name="DetailsPresenter"
                                        SelectiveScrollingGrid.SelectiveScrollingOrientation="{TemplateBinding primitives:DataGridRowHelper.AreRowDetailsFrozen, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, Converter={x:Static DataGrid.RowDetailsScrollingConverter}}"
                                        Visibility="{TemplateBinding DetailsVisibility}" />
                                        </Border>
                                        <DataGridRowHeader
                                    x:Name="RowHeader"
                                    Grid.RowSpan="2"
                                    SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                                    Visibility="{TemplateBinding primitives:DataGridRowHelper.HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Row}, Converter={x:Static DataGrid.HeadersVisibilityConverter}}" />
                                    </SelectiveScrollingGrid>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="MouseOver">
                                    <Setter TargetName="BackgroundRectangle" Property="Fill" Value="{DynamicResource SystemControlHighlightListLowBrush}" />
                                </Trigger>
                                <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="Normal_Selected">
                                    <Setter TargetName="BackgroundRectangle" Property="Fill" Value="{DynamicResource DataGridRowSelectedBackground}" />
                                    <Setter TargetName="BackgroundRectangle" Property="Opacity" Value="{DynamicResource DataGridRowSelectedBackgroundOpacity}" />
                                </Trigger>
                                <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="MouseOver_Selected">
                                    <Setter TargetName="BackgroundRectangle" Property="Fill" Value="{DynamicResource DataGridRowSelectedHoveredBackground}" />
                                    <Setter TargetName="BackgroundRectangle" Property="Opacity" Value="{DynamicResource DataGridRowSelectedHoveredBackgroundOpacity}" />
                                </Trigger>
                                <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="MouseOver_Unfocused_Selected">
                                    <Setter TargetName="BackgroundRectangle" Property="Fill" Value="{DynamicResource DataGridRowSelectedHoveredUnfocusedBackground}" />
                                    <Setter TargetName="BackgroundRectangle" Property="Opacity" Value="{DynamicResource DataGridRowSelectedHoveredUnfocusedBackgroundOpacity}" />
                                </Trigger>
                                <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="Unfocused_Selected">
                                    <Setter TargetName="BackgroundRectangle" Property="Fill" Value="{DynamicResource DataGridRowSelectedUnfocusedBackground}" />
                                    <Setter TargetName="BackgroundRectangle" Property="Opacity" Value="{DynamicResource DataGridRowSelectedUnfocusedBackgroundOpacity}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsNewItem" Value="True">
                        <Setter Property="Margin" Value="{Binding NewItemMargin, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="TreeGridStyle" BasedOn="{StaticResource DefaultDataGridStyle}" TargetType="uictrl:PhysicalQueryPlanTreeGrid">
                <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            </Style>

            <Style x:Key="LogicalTreeGridStyle" BasedOn="{StaticResource DefaultDataGridStyle}" TargetType="uictrl:LogicalQueryPlanTreeGrid">
                <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            </Style>

            <!-- Custom style for filter toggle buttons that maintains background when disabled -->
            <Style x:Key="FilterToggleButtonStyle" BasedOn="{StaticResource TraceToggleButton}" TargetType="ToggleButton">
                <Style.Triggers>
                    <MultiTrigger>
                        <MultiTrigger.Conditions>
                            <!--<Condition Property="IsChecked" Value="False"/>-->
                            <Condition Property="IsEnabled" Value="False"/>
                        </MultiTrigger.Conditions>
                        <Setter Property="Opacity" Value="0.3"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="IsHitTestVisible" Value="False"/>
                    </MultiTrigger>
                    
                    
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
        
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="{Binding ShowPhysicalQueryPlan, Converter={StaticResource BoolToGridRowHeightConverter}, ConverterParameter=2}"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="{Binding ShowLogicalQueryPlan, Converter={StaticResource BoolToGridRowHeightConverter}, ConverterParameter=1}"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TraceStatusText"
                   Grid.ColumnSpan="5"
                   Grid.Row="1"
                   Text="{Binding TraceStatusText}"
                   Visibility="{Binding TraceStatusText, Converter={StaticResource StringToVisibilityConverter}}" 
                   Background="{DynamicResource WarningBarBackgroundBrush}" 
                   Foreground="{StaticResource MutedTextBrush}" Padding="5,1,0,1"/>




            <!-- command bar -->
        <controls:ClipBorder
                CornerRadius="4"
                Background="{DynamicResource Theme.Brush.MenuBar.Back}"
                Margin="0 0 0 8">
            <DockPanel LastChildFill="False">
                <ToggleButton DockPanel.Dock="Left" Style="{StaticResource TraceToggleButton}" IsChecked="{Binding IsRecording}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{DynamicResource record_toolbarDrawingImage}"
                               Margin="0 0 4 0"/>
                        <TextBlock>Record</TextBlock>
                    </StackPanel>
                </ToggleButton>
                <ToggleButton DockPanel.Dock="Left" Style="{StaticResource TraceToggleButton}" IsChecked="{Binding IsPaused}"  IsEnabled="{Binding CanPause}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{DynamicResource pause_toolbarDrawingImage}"
                               Margin="0 0 4 0"/>
                        <TextBlock>Pause</TextBlock>
                    </StackPanel>
                </ToggleButton>

                <ToggleButton DockPanel.Dock="Left" Style="{StaticResource TraceToggleButton}" IsChecked="{Binding IsStopped}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{DynamicResource cancelDrawingImage}" Width="16"
                               Margin="0 0 4 0"/>
                        <TextBlock>Stop</TextBlock>
                    </StackPanel>
                </ToggleButton>
                <Button DockPanel.Dock="Left" Style="{StaticResource TraceButton}"
                        cal:Message.Attach="[Event Click] = [Action ClearAll]">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{DynamicResource clear_toolbarDrawingImage}"
                               Margin="0 0 4 0"/>
                        <TextBlock>Clear</TextBlock>
                    </StackPanel>
                </Button>

                <Button DockPanel.Dock="Left" Style="{StaticResource TraceButton}"
                            cal:Message.Attach="[Event Click] = [Action Export]">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{DynamicResource save_toolbarDrawingImage}"
                               Margin="0 0 4 0"/>
                        <TextBlock>Export</TextBlock>
                    </StackPanel>
                </Button>

                <Button DockPanel.Dock="Left" Style="{StaticResource TraceButton}"
                        cal:Message.Attach="[Event Click] = [Action ShowTraceDiagnostics]">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{DynamicResource aboutDrawingImage}"
                               Width="16" Height="16"
                               Margin="0 0 4 0"/>
                        <TextBlock>Info</TextBlock>
                    </StackPanel>
                </Button>
                <!--<Rectangle DockPanel.Dock="Right"/>-->
                

                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" >
                        <ToggleButton x:Name="ShowLogicalPlan" 
                              IsChecked="{Binding ShowLogicalQueryPlan}"
                              Style="{StaticResource TraceToggleButton}"
                              IsEnabled="{Binding CanHideLogicalQueryPlan}"
                              Margin="2 2 0 2"
                              Padding="4 4 28 4">
                        <TextBlock VerticalAlignment="Center">Logical Plan</TextBlock>
                    </ToggleButton>
                    
                    <ToggleButton 
                        Style="{StaticResource FilterToggleButtonStyle}" 
                        Margin="-24 2 4 2" 
                        Padding="1"
                        ToolTip="Show Filters for Logical Query Plan" 
                        IsChecked="{Binding ShowLogicalQueryPlanFilters}"
                        IsEnabled="{Binding ShowLogicalQueryPlan}">
                        <Image Source="{DynamicResource filter_toolbarDrawingImage}"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               Height="16"
                               Width="16"
                               Margin="0"/>
                    </ToggleButton>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" >
                    <ToggleButton x:Name="ShowPhysicalPlan" 
                              Style="{StaticResource TraceToggleButton}"
                              IsChecked="{Binding ShowPhysicalQueryPlan}"
                              IsEnabled="{Binding CanHidePhysicalQueryPlan}"
                              Margin="2 2 0 2"
                              Padding="4 4 28 4">
                        <TextBlock VerticalAlignment="Center">Physical Plan</TextBlock>
                    </ToggleButton>
                    
                    <ToggleButton 
                        x:Name="ShowPhysicalQueryPlanFilters"
                        Style="{StaticResource FilterToggleButtonStyle}" 
                        Margin="-24 2 4 2" 
                        Padding="1"
                        ToolTip="Show Filters for Physical Query Plan" 
                        IsChecked="{Binding ShowPhysicalQueryPlanFilters}"
                        IsEnabled="{Binding ShowPhysicalQueryPlan}">
                        <Image Source="{DynamicResource filter_toolbarDrawingImage}"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               Height="16"
                               Width="16"/>
                    </ToggleButton>
                </StackPanel>

            </DockPanel>
        </controls:ClipBorder>

        <controls:ClipBorder Grid.Row="2"
                             CornerRadius="4"
                             BorderThickness="1"
                             Style="{StaticResource BusyBlur}"
                             BorderBrush="{DynamicResource Theme.Brush.Control.Border}">

            <uictrl:PhysicalQueryPlanTreeGrid x:Name="PhysicalTreeGrid"
                    RootItems="{Binding PhysicalQueryPlanTree}"
                    ChildrenBindingPath="Children"
                    ExpandOnLoad="True"
                    FrozenColumnCount="2"
                    GridLinesVisibility="None" 
                    AlternationCount="2"
                    HorizontalScrollBarVisibility="Auto"
                    SelectedValue="{Binding SelectedPhysicalQueryPlanRow}"
                    cal:Message.Attach="[Event Sorting] = [Action OnSorting($source, $eventArgs)]"
                    RowStyle="{StaticResource DefaultDataGridRowStyle}"
                    AlternatingRowBackground="{DynamicResource Theme.Brush.Table.Alternate}"
                    Background="{DynamicResource Theme.Brush.Content.Back}"
                    Style="{StaticResource TreeGridStyle}"
                    CustomDescendantFilter="{Binding FindDescendantsWithHigherRecordCountsFunc}"  
                    ShowDefaultContextMenu="True"
                    EnableRowVirtualization="True"
                    IsReadOnly="True"
                    filter:DataGridExtensions.UseBackgroundWorkerForFiltering="True"
                    filter:DataGridExtensions.IsClearButtonVisible="False"
                    filter:DataGridExtensions.IsFilterVisible="{Binding ShowPhysicalQueryPlanFilters}" 
                    filter:DataGridExtensions.HasActiveFilters="{Binding HasPhysicalFilterActive, Mode=OneWayToSource}"
                    ColumnHeaderStyle="{StaticResource {ComponentResourceKey 
		                    TypeInTargetAssembly={x:Type filter:DataGridHeaderFilterControl}, 
		            ResourceId=DataGridHeaderFilterControlStyle}}"
                >
                <uictrl:PhysicalQueryPlanTreeGrid.ContextMenu>
                    <ContextMenu cal:Action.TargetWithoutContext="{Binding Path=PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <Separator/>

                        <MenuItem Header="Drill In"
                                  cal:Action.TargetWithoutContext="{Binding Source={StaticResource Proxy}, Path=Data}"
                                  cal:Message.Attach="[Event Click] = [Action DrillIn($dataContext)]"
                                  />
                        <MenuItem Header="Drill Out"
                                  cal:Action.TargetWithoutContext="{Binding Source={StaticResource Proxy}, Path=Data}"
                                  cal:Message.Attach="[Event Click] = [Action DrillOut($dataContext)]"
                                  />
                    </ContextMenu>
                </uictrl:PhysicalQueryPlanTreeGrid.ContextMenu>
                <uictrl:PhysicalQueryPlanTreeGrid.Columns>
                    <DataGridTextColumn Header="Line" Width="60" 
                                        IsReadOnly="True" 
                                        Binding="{Binding Data.RowNumber, StringFormat='#,#;;'}" ElementStyle="{StaticResource AlignRight}"
                                        filter:DataGridColumnExtensions.DoNotGenerateFilterControl="True"/>
                    <DataGridTemplateColumn Header="Records" Width="100" 
                                            IsReadOnly="True" 
                                            SortMemberPath="Data.Records"
                                            filter:DataGridColumnExtensions.CustomBindingPath="Data.Records">
                        <DataGridTemplateColumn.CellTemplate>
                        
                        <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Data.Records, StringFormat='#,#;;'}" 
                                           HorizontalAlignment="Right"
                                               VerticalAlignment="Center"
                                               >
                                    
                        <TextBlock.Foreground>
                            <MultiBinding Converter="{StaticResource AncestorRecordsColorConverter}">
                                <Binding Path="Data.Records" />
                                <Binding Path="Data.SelectedAncestorRecords" />
                            </MultiBinding>
                        </TextBlock.Foreground>
                                </TextBlock>
                                    <Image Grid.Column="1"
                                           VerticalAlignment="Center"
                                           Width="12" Height="12"
                                           Margin="0 0 0 0"
                                           >
                                        <Image.Source >
                                            <MultiBinding Converter="{StaticResource AncestorRecordsImageResourceConverter}">
                                                <Binding Path="Data.Records" />
                                                <Binding Path="Data.SelectedAncestorRecords" />
                                            </MultiBinding>
                                        </Image.Source>
                                    </Image>
                                    
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <ctrls:TreeColumn 
                        Header="Physical Query Plan"
                        TextPath="Data.Operation"
                        Width="Auto"
                        ClipboardContentBinding="{Binding Data.IndentedOperation}"
                        filter:DataGridColumnExtensions.CustomBindingPath="Data.Operation"
                        filter:DataGridColumnExtensions.IsContainsTextSearch="True"
                        ShowTreeLines="{Binding DataContext.ShowPhysicalTreeLines, RelativeSource={RelativeSource AncestorType={x:Type uictrl:PhysicalQueryPlanTreeGrid}}, Mode=OneWay}"
                        ShowExpander="{Binding DataContext.ShowPhysicalTreeLines, RelativeSource={RelativeSource AncestorType={x:Type uictrl:PhysicalQueryPlanTreeGrid}}, Mode=OneWay}"
                        TextForeground="{DynamicResource Theme.Brush.Default.Fore}"
                        SelectedLineStroke="{DynamicResource Theme.Brush.Icons.Red}"
                        LineStroke="{DynamicResource Theme.Brush.Muted.Fore}"
                        >


                        <ctrls:TreeColumn.TextTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding .}">
                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}">
                                            <Style.Triggers>
                                            <DataTrigger Binding="{Binding DataContext.Data.HighlightRow, RelativeSource={RelativeSource AncestorType={x:Type ctrls:TreeCell}}}" Value="True">
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                </TextBlock>

                            </DataTemplate>
                        </ctrls:TreeColumn.TextTemplate>
                    </ctrls:TreeColumn>

                </uictrl:PhysicalQueryPlanTreeGrid.Columns>
            </uictrl:PhysicalQueryPlanTreeGrid>
            
        </controls:ClipBorder>
        <GridSplitter Grid.Row="3" Height="5"  ResizeDirection="Rows" 
            Grid.Column="0"
            Width="Auto" 
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch" 
            Background="Transparent"
            Margin="0"/>
        <controls:ClipBorder Grid.Row="4"
                             CornerRadius="4"
                             BorderThickness="1"
                             BorderBrush="{DynamicResource Theme.Brush.Control.Border}"
                             Style="{StaticResource BusyBlur}">

            <uictrl:LogicalQueryPlanTreeGrid
                x:Name="LogicalTreeGrid"
                RootItems="{Binding LogicalQueryPlanTree}"
                ChildrenBindingPath="Children"
                ExpandOnLoad="True"
                FrozenColumnCount="1"
                GridLinesVisibility="None" 
                AlternationCount="2"
                HorizontalScrollBarVisibility="Auto"
                RowStyle="{StaticResource DefaultDataGridRowStyle}"
                AlternatingRowBackground="{DynamicResource Theme.Brush.Table.Alternate}"
                Background="{DynamicResource Theme.Brush.Content.Back}"
                Style="{StaticResource LogicalTreeGridStyle}"
                EnableRowVirtualization="True"
                IsReadOnly="True"
                filter:DataGridExtensions.UseBackgroundWorkerForFiltering="True"
                filter:DataGridExtensions.IsClearButtonVisible="False"
                filter:DataGridExtensions.IsFilterVisible="{Binding ShowLogicalQueryPlanFilters}" 
                filter:DataGridExtensions.HasActiveFilters="{Binding HasLogicalFilterActive, Mode=OneWayToSource}"
                ColumnHeaderStyle="{StaticResource {ComponentResourceKey 
                TypeInTargetAssembly={x:Type filter:DataGridHeaderFilterControl}, 
                ResourceId=DataGridHeaderFilterControlStyle}}"
                >
                <uictrl:LogicalQueryPlanTreeGrid.Columns>
                    <DataGridTextColumn Header="Line" Width="60" IsReadOnly="True" Binding="{Binding Data.RowNumber, StringFormat='#,#;;'}" ElementStyle="{StaticResource AlignRight}" 
                                        filter:DataGridColumnExtensions.DoNotGenerateFilterControl="True"/>

                    <ctrls:TreeColumn 
                        Header="Logical Query Plan"
                        Width="Auto"
                        IsReadOnly="True"
                        TextPath="Data.Operation"
                        ClipboardContentBinding="{Binding Data.IndentedOperation}"
                        ShowTreeLines="{Binding DataContext.ShowLogicalTreeLines, RelativeSource={RelativeSource AncestorType={x:Type uictrl:LogicalQueryPlanTreeGrid}}, Mode=OneWay}"
                        ShowExpander="{Binding DataContext.ShowLogicalTreeLines, RelativeSource={RelativeSource AncestorType={x:Type uictrl:LogicalQueryPlanTreeGrid}}, Mode=OneWay}"
                        filter:DataGridColumnExtensions.CustomBindingPath="Data.Operation"
                        filter:DataGridColumnExtensions.IsContainsTextSearch="True"
                        TextForeground="{DynamicResource Theme.Brush.Default.Fore}"
                        SelectedLineStroke="{DynamicResource Theme.Brush.Icons.Red}"
                        LineStroke="{DynamicResource Theme.Brush.Muted.Fore}"
                        >

                    </ctrls:TreeColumn>
                    
                </uictrl:LogicalQueryPlanTreeGrid.Columns>
            </uictrl:LogicalQueryPlanTreeGrid>
            
        </controls:ClipBorder>
        
        <!-- Busy overlay -->
        <Border 
            Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" 
            Grid.Row="1"
            Grid.RowSpan="4" Style="{StaticResource BusyPanel}">
            <Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                    <ProgressBar 
                                    x:Name="spinner"
                                    Style="{StaticResource MaterialDesignCircularProgressBar}" 
                                    Grid.Column="0"
                                    Width="24"
                                    Height="24"
                                    Margin="0 0 10 0"
                                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <Label x:Name="BusyMessage"  Style="{StaticResource SpinnerLabel}"/>
                    
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</controls:ZoomableUserControl>
